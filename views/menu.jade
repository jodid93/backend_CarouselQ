extends layout

block content
	h1 vertu velkominn #{user}
	form(method='post', action='/')
		div: button(type='submit') Add random user

	form(method='get', action='/testConnection')
		div: button(type='submit') test Connection
			

	if userExsist
		h1 #{randomUser} has been added

